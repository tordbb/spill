# v2

The deployed `/v2/` site is generated by `.github/workflows/pages.yml` from the final stable root build, then receives the v2-only responsive city overlay from `site-overlays/inject-city-v2.py`.

This keeps the front page and games in `/v2/` in sync with the stable root while isolating the city layout experiment from `/spill/`.
