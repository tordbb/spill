<!DOCTYPE html>
<html lang="no">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1, minimum-scale=0.5, maximum-scale=5, user-scalable=yes">
<title>Lekeverkstedet 🌟</title>
<style>
  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; user-select:none; -webkit-user-select:none; }
  html,body { height:100%; overflow:hidden; overscroll-behavior:none; font-family:system-ui,-apple-system,"Segoe UI",sans-serif; }
  body { background:linear-gradient(160deg,#c7ecff 0%,#e6f7ff 40%,#fff4d6 100%); touch-action:pinch-zoom; }

  .screen { display:none; position:fixed; inset:0; flex-direction:column; align-items:center; }
  .screen.active { display:flex; }

  /* ---------- buttons ---------- */
  .icon-btn {
    border:none; background:#ffffffcc; border-radius:22px; box-shadow:0 4px 0 #00000018;
    font-size:34px; width:64px; height:64px; display:flex; align-items:center; justify-content:center;
    cursor:pointer; transition:transform .12s;
  }
  .icon-btn:active { transform:scale(.9); }
  .top-bar { position:absolute; top:10px; left:10px; right:10px; display:flex; justify-content:space-between; align-items:center; z-index:20; pointer-events:none; }
  .top-bar > * { pointer-events:auto; }

  /* ---------- home ---------- */
  #home h1 { margin-top:4vh; font-size:min(9vw,52px); color:#2b6cb0; text-shadow:0 2px 0 #ffffff; letter-spacing:2px; }
  #home .subtitle { font-size:30px; margin-top:2px; }
  .menu-grid { display:grid; grid-template-columns:repeat(2,minmax(140px,220px)); gap:min(4vw,26px); margin-top:4vh; padding:10px; }
  @media (orientation:landscape) {
    #home h1 { margin-top:2px; font-size:min(7vh,38px); }
    .menu-grid {
      grid-template-columns:repeat(3,minmax(0,1fr));
      width:min(94vw,calc((100vh - 72px) * 1.5));
      gap:min(2vh,10px); margin-top:4px; padding:4px;
    }
    .menu-card { border-radius:20px; font-size:min(12vh,54px); }
    .menu-card .mini { font-size:min(5vh,22px); }
  }
  .menu-card {
    aspect-ratio:1/1; border:none; border-radius:28px; cursor:pointer; position:relative;
    display:flex; flex-direction:column; align-items:center; justify-content:center; gap:6px;
    box-shadow:0 8px 0 #00000022; transition:transform .12s; font-size:min(13vw,64px);
  }
  .menu-card:active { transform:translateY(4px) scale(.97); }
  .menu-card .mini { font-size:min(5vw,26px); opacity:.95; }
  .menu-card .prog { position:absolute; bottom:10px; display:flex; gap:4px; }
  .menu-card .prog span { width:10px; height:10px; border-radius:50%; background:#ffffff88; }
  .menu-card .prog span.done { background:#ffd54a; box-shadow:0 0 4px #ffb300; }
  #card-bal { background:linear-gradient(145deg,#b388ff,#7c4dff); }
  #card-cre { background:linear-gradient(145deg,#ffab91,#ff7043); }
  #card-pat { background:linear-gradient(145deg,#80deea,#26a69a); }
  #card-wal { background:linear-gradient(145deg,#aed581,#7cb342); }
  #btn-book { position:relative; }
  #book-count { position:absolute; top:-6px; right:-6px; background:#ff5252; color:#fff; font-size:15px; font-weight:bold;
    border-radius:50%; min-width:24px; height:24px; display:flex; align-items:center; justify-content:center; padding:0 4px; }

  /* ---------- level dots ---------- */
  .dots { display:flex; gap:8px; justify-content:center; margin:0 auto; flex-wrap:wrap; max-width:70vw; }
  .dots button { border:none; width:30px; height:30px; border-radius:50%; font-size:15px; cursor:pointer;
    background:#ffffff99; color:#0000; box-shadow:0 2px 0 #00000015; }
  .dots button.done { color:#000; background:#ffe082; font-size:16px; }
  .dots button.cur { outline:3px solid #42a5f5; color:#000; background:#fff; }
  .dots button.locked { opacity:.35; }

  /* ---------- shared game layout ---------- */
  .stage-wrap { flex:1; width:100%; display:flex; flex-direction:column; align-items:center; justify-content:center; padding:84px 8px 8px; min-height:0; }

  /* ---------- award overlay ---------- */
  #award { display:none; position:fixed; inset:0; z-index:50; background:#00000055; backdrop-filter:blur(3px);
    align-items:center; justify-content:center; flex-direction:column; }
  #award.show { display:flex; }
  #award .card { background:#fff; border-radius:36px; padding:34px 50px; display:flex; flex-direction:column; align-items:center; gap:8px;
    animation:popin .45s cubic-bezier(.2,1.6,.4,1); box-shadow:0 14px 40px #00000044; }
  #award .stk { font-size:110px; animation:wob 1.4s ease-in-out infinite; }
  #award .stars { font-size:34px; }
  #award .go { font-size:44px; margin-top:6px; animation:pulse 1.1s infinite; }
  @keyframes popin { from { transform:scale(.2) rotate(-14deg); opacity:0; } }
  @keyframes wob { 0%,100% { transform:rotate(-6deg);} 50% { transform:rotate(6deg);} }
  @keyframes pulse { 0%,100% { transform:scale(1);} 50% { transform:scale(1.18);} }

  /* ---------- cheer (gentle encouragement) ---------- */
  #cheer { position:fixed; left:50%; top:42%; transform:translate(-50%,-50%); z-index:49; font-size:86px;
    pointer-events:none; opacity:0; }
  #cheer.show { animation:cheerin 1.5s ease forwards; }
  @keyframes cheerin { 0% { opacity:0; transform:translate(-50%,-30%) scale(.4);} 18% { opacity:1; transform:translate(-50%,-50%) scale(1.15);} 30% { transform:translate(-50%,-50%) scale(1);} 80% { opacity:1;} 100% { opacity:0; transform:translate(-50%,-70%) scale(1);} }

  /* ---------- confetti ---------- */
  #fx { position:fixed; inset:0; z-index:60; pointer-events:none; }

  /* ---------- all-done crown ---------- */
  .crown { position:absolute; top:6px; right:10px; font-size:30px; filter:drop-shadow(0 2px 2px #0003); }

  .hintpulse { animation:hintp 1s ease-in-out infinite; }
  @keyframes hintp { 0%,100% { box-shadow:0 0 0 0 #ffd54a; } 50% { box-shadow:0 0 0 10px #ffd54a55, 0 0 18px 6px #ffd54a; } }

  .shake { animation:shk .5s; }
  @keyframes shk { 0%,100%{transform:translateX(0)} 25%{transform:translateX(-7px) rotate(-4deg)} 75%{transform:translateX(7px) rotate(4deg)} }

  /* =================== BALANCE =================== */
  #g-bal .stage-wrap { justify-content:center; gap:16px; }
  #bal-boxes { display:flex; align-items:stretch; justify-content:center; gap:8px; width:min(96vw,760px); }
  .bbox { flex:1; max-width:44%; min-height:min(38vh,300px);
    background:linear-gradient(#f2d3a3,#e0b478); border:6px solid #b07f46; border-radius:16px;
    box-shadow:inset 0 12px 18px #00000026, inset 0 -6px 0 #00000012, 0 6px 0 #00000018;
    display:flex; flex-wrap:wrap; align-content:flex-start; justify-content:center; gap:4px; padding:16px 8px; }
  #bal-eq { align-self:center; font-size:min(10vw,46px); filter:drop-shadow(0 2px 2px #0002); }
  #bal-tray { width:min(96vw,760px); min-height:86px; background:#ffffffaa; border-radius:20px; box-shadow:0 4px 0 #00000012;
    display:flex; align-items:center; justify-content:center; flex-wrap:wrap; gap:10px; padding:8px; }
  .bitem { font-size:min(11vw,46px); line-height:1; padding:2px; cursor:grab; transition:transform .15s; }
  .bitem.tapme:active { transform:scale(1.2); }
  .bitem.dragging { opacity:.25; }
  .bitem.cancelpulse { animation:cpul .8s ease-in-out infinite; border-radius:50%; }
  .inv { filter:invert(1); } /* anti-version: identical symbol, inverted colors */
  @keyframes cpul { 0%,100% { transform:scale(1); filter:drop-shadow(0 0 0 #fff);} 50% { transform:scale(1.22); filter:drop-shadow(0 0 10px #ffd54a);} }
  .fly { position:fixed; left:0; top:0; z-index:69; font-size:min(11vw,46px); pointer-events:none;
    transition:transform .5s cubic-bezier(.35,.7,.35,1), opacity .5s; }
  #drag-ghost { position:fixed; z-index:70; pointer-events:none; font-size:min(12vw,52px); transform:translate(-50%,-60%); filter:drop-shadow(0 6px 8px #0004); }

  /* =================== CREATURE =================== */
  #cre-svg { width:min(92vw,58vh,560px); aspect-ratio:1/1; background:#eef9ff; border-radius:26px; box-shadow:0 6px 0 #00000015; touch-action:pinch-zoom; }
  .slot { fill:#9fb4c4; opacity:.5; }
  .slot.glow { opacity:.85; fill:#ffd54a; }
  .cpart { cursor:grab; filter:drop-shadow(0 2px 2px #0003); }
  .alive { animation:wob 1.2s ease-in-out infinite; transform-origin:50px 40px; }
  #cre-tools { display:flex; gap:10px; margin-top:8px; }

  /* =================== PATH =================== */
  #pat-grid { display:grid; gap:4px; background:#a5d6a7; padding:10px; border-radius:22px; box-shadow:0 6px 0 #00000018; position:relative; touch-action:pinch-zoom; }
  .ptile { width:var(--ts); height:var(--ts); border-radius:10px; background:#c8e6c9; display:flex; align-items:center; justify-content:center;
    font-size:calc(var(--ts) * .72); }
  .ptile.wall { background:#81c784; }
  #pat-cat { position:absolute; width:var(--ts); height:var(--ts); display:flex; align-items:center; justify-content:center;
    font-size:calc(var(--ts) * .8); transition:transform .38s ease; z-index:5; }
  .paw { position:absolute; display:flex; align-items:center; justify-content:center; width:var(--ts); height:var(--ts);
    font-size:calc(var(--ts) * .45); opacity:.75; z-index:4; pointer-events:none; }
  #pat-controls { display:flex; flex-direction:column; align-items:center; gap:8px; margin-top:10px; width:100%; }
  #pat-queue { min-height:56px; max-width:94vw; background:#ffffffaa; border-radius:16px; display:flex; align-items:center; flex-wrap:wrap;
    gap:4px; padding:6px 10px; justify-content:center; min-width:min(80vw,420px); }
  .qchip { font-size:34px; background:#fff; border-radius:12px; padding:2px 6px; box-shadow:0 3px 0 #00000015; cursor:pointer; transition:transform .2s; }
  .qchip.exec { transform:scale(1.28); background:#fff3bf; box-shadow:0 0 14px 4px #ffd54a, 0 3px 0 #00000015; }
  .foe { position:absolute; width:var(--ts); height:var(--ts); display:flex; align-items:center; justify-content:center;
    font-size:calc(var(--ts) * .8); transition:transform .42s ease; z-index:5; }
  .foe .bonebadge { display:none; position:absolute; right:-4px; bottom:-2px; font-size:calc(var(--ts) * .4); }
  .foe.flee .bonebadge { display:block; }
  #pat-cat .catbone { position:absolute; right:-2%; top:-7%; font-size:calc(var(--ts) * .42); line-height:1;
    filter:drop-shadow(0 1px 1px #0004); pointer-events:none; }

  /* =================== CITY =================== */
  #cit-hud { display:flex; gap:16px; align-items:center; background:#ffffffcc; border-radius:16px; padding:8px 16px;
    font-size:24px; box-shadow:0 3px 0 #00000012; }
  #cit-hud b { margin-left:4px; }
  #cit-week { display:flex; gap:4px; align-items:center; }
  #cit-week span { width:13px; height:13px; border-radius:50%; background:#cfd8dc; }
  #cit-week span.wk { background:#81c784; }
  #cit-week span.today { outline:3px solid #42a5f5; }
  #g-cit .stage-wrap { flex-direction:row; align-items:center; justify-content:center; gap:8px; padding:4px 4px 4px 72px; }
  #cit-left-nav { position:absolute; top:10px; left:10px; z-index:30; display:flex; flex-direction:column; gap:8px; }
  #cit-left-nav .icon-btn { width:58px; height:58px; font-size:31px; }
  /* The city keeps a landscape-shaped game surface even when the device itself is held upright. */
  @media (orientation:portrait) {
    #g-cit.active { inset:auto; left:100vw; top:0; width:100vh; height:100vw; transform:rotate(90deg); transform-origin:0 0; }
  }
  #cit-grid { position:relative; border-radius:14px; box-shadow:0 6px 0 #00000018; touch-action:pinch-zoom; overflow:hidden;
    background-color:#6f9e58; /* default ground: dark bushy green (placeable grass tile is lighter) */
    background-image:radial-gradient(#00000022 16%, transparent 17%), linear-gradient(#00000012 1px, transparent 1px), linear-gradient(90deg, #00000012 1px, transparent 1px); }
  .ct { position:absolute; display:flex; align-items:center; justify-content:center; box-sizing:border-box; pointer-events:none; }
