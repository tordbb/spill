--- a/index.html
+++ b/index.html
@@ -185,9 +185,10 @@
   @keyframes sdkpop { from{transform:scale(.15) rotate(-15deg)} to{transform:scale(1)} }
   @keyframes sdkhint { 0%,100%{outline:0 solid #fff0} 50%{outline:5px solid #fff; box-shadow:0 0 0 8px #ffd54a88} }
   #sdk-modes { display:flex; align-items:center; justify-content:center; gap:10px; min-height:52px; flex-wrap:nowrap; }
-  .sdk-mode { width:72px; height:48px; border:0; border-radius:16px; background:#ffffffc9; font-size:27px;
-    box-shadow:0 4px 0 #00000018; cursor:pointer; }
-  .sdk-mode.sel { outline:4px solid #42a5f5; background:#fff; transform:translateY(-1px); }
+  .sdk-mode { width:72px; height:48px; border:0; border-radius:16px; background:#8b939b; color:#fff; font-size:27px;
+    box-shadow:0 4px 0 #00000028; cursor:pointer; }
+  .sdk-mode.sel { outline:4px solid #42a5f5; background:#69737c; color:#fff; transform:translateY(-1px); }
+  #sdk-mode-x { font-family:system-ui,sans-serif; font-weight:950; font-size:34px; line-height:1; }
   @media (max-height:430px) {
     #g-sdk .stage-wrap { padding-top:95px; gap:4px; }
     #sdk-count { min-height:38px; padding:1px 9px; font-size:15px; border-radius:10px; }
@@ -282,6 +283,10 @@
     padding:1px 4px; font-weight:bold; box-shadow:0 2px 0 #00000018; }
   .cbtn.undo:disabled { opacity:.32; filter:grayscale(1); }
   #cit-right { display:flex; flex:0 0 auto; flex-direction:column; align-items:center; gap:10px; background:#ffffffaa; padding:8px; border-radius:18px; max-height:calc(100% - 8px); }
+  /* In portrait the whole city surface is rotated. Reserve one extra HUD-width beyond the HUD
+     so browser address/navigation chrome cannot sit on top of the money/day controls. */
+  #cit-safe-spacer { display:none; flex:0 0 auto; height:1px; pointer-events:none; }
+  @media (orientation:portrait) { #cit-safe-spacer { display:block; } }
   #cit-pop-wrap { font-size:20px; }
   #cit-edit-actions { display:flex; gap:5px; align-items:center; justify-content:center; }
   #cit-edit-actions .icon-btn { width:44px; height:44px; border-radius:14px; font-size:23px; }
@@ -469,14 +474,14 @@
   <div class="game-help" id="sdk-help">
     <span>Finn ett dyr i hver farge, rad og kolonne</span>
     <span>Dyr kan ikke stå ved siden av hverandre</span>
-    <span>❌ stryk • 🐾 finn</span>
+    <span>× stryk • 🐾 finn</span>
   </div>
   <div class="top-bar"><button class="icon-btn nav-home">🏡</button><div class="dots" id="sdk-dots"></div><button class="icon-btn" id="sdk-reset">↺</button></div>
   <div class="stage-wrap">
     <div id="sdk-board"></div>
     <div id="sdk-modes">
       <div id="sdk-count"><span id="sdk-count-animal">🦝</span><b id="sdk-found-count">0 / 6</b></div>
-      <button class="sdk-mode sel" id="sdk-mode-x" aria-label="Stryk ut">❌</button>
+      <button class="sdk-mode sel" id="sdk-mode-x" aria-label="Stryk ut">×</button>
       <button class="sdk-mode" id="sdk-mode-animal" aria-label="Finn dyr">🐾</button>
     </div>
   </div>
@@ -499,6 +504,7 @@
       <div id="cit-edit-actions"><button class="icon-btn" id="cit-clear">🗑️</button></div>
       <button class="icon-btn" id="cit-night" style="background:linear-gradient(145deg,#3949ab,#1a237e);">🌙</button>
     </div>
+    <div id="cit-safe-spacer" aria-hidden="true"></div>
   </div>
   <div id="cit-fade">🌙</div>
   <div id="cit-clear-confirm">
@@ -1630,44 +1636,41 @@
 /* ================= ANIMAL SUDOKU ================= */
 /* One animal in every row, column and coloured region; animals may not touch.
    ❌ toggles notes only on empty cells. 🐾 checks an animal candidate.
-   Correct animals become permanent and automatically cross out impossible cells. */
-const SDK_BASE_REGIONS=[
-  'ABCCCD',
-  'AACCDD',
-  'AADDDD',
-  'AAADDD',
-  'EEFFDD',
-  'FFFFFF'
-];
-const SDK_BASE_SOLUTION=[1,3,5,2,0,4];
-const SDK_PALETTES=[
+   Correct animals become permanent. X marks are manual notes and may intentionally be wrong. */
+const SDK_LEVELS=[
+  {regions:['DBBAAC','DBBBBC','DDFCCC','DDFFFF','DDFEFF','DFFFFF'],solution:[4,2,5,0,3,1]},
+  {regions:['AABBBB','ABBBBB','ACCBFF','ACEEFD','ACEEFF','AEEFFF'],solution:[0,3,1,5,2,4]},
+  {regions:['BBBBBA','BBCCCC','DDCCCC','DDECFF','EEECCF','EEEFFF'],solution:[5,1,3,0,2,4]},
+  {regions:['AAAAAA','BBBBAA','BBCBDD','BBEDDD','EEEEDD','FFFEDD'],solution:[4,0,2,5,3,1]},
+  {regions:['AAABBB','CCABBB','CBBBBD','CFFDDD','CFFFDE','CCFFFF'],solution:[1,4,0,3,5,2]},
+  {regions:['AAAACC','AAABDC','DDDDDC','DDDFFC','EEFFCC','EEEFFF'],solution:[1,3,5,2,0,4]},
+  {regions:['BBBCCA','BCBCCD','BCCCCD','EECDDD','EEEFFD','FFFFDD'],solution:[5,0,2,4,1,3]},
+  {regions:['AAAACC','DAABCC','DDACCC','DFFFEC','FFEEEE','FFFFEE'],solution:[1,3,5,0,4,2]}
+].map((lv,i)=>Object.assign({},lv,{animal:['🦝','🐼','🐰','🐸','🦊','🐵','🐧','🐨'][i],palette:[
   ['#8f6ad8','#f5cf2f','#a8e58c','#e97ade','#5dc0e5','#c94f88'],
   ['#65c6b5','#ffd166','#8bcf72','#ef8eb8','#7aa5e6','#b56bd8'],
   ['#ffad77','#80cbc4','#b39ddb','#f7d36d','#81d4fa','#e78ac3'],
   ['#9ccc65','#ffcc80','#90caf9','#ce93d8','#ef9a9a','#80cbc4']
-];
-const SDK_ANIMALS=['🦝','🐼','🐰','🐸','🦊','🐵','🐧','🐨'];
+][i%4]}));
 
-function sdkTransformCell(r,c,t){
-  const n=6;
-  if(t===0)return[r,c];
-  if(t===1)return[c,n-1-r];
-  if(t===2)return[n-1-r,n-1-c];
-  if(t===3)return[n-1-c,r];
-  if(t===4)return[r,n-1-c];
-  if(t===5)return[n-1-r,c];
-  if(t===6)return[c,r];
-  return[n-1-c,n-1-r];
-}
-function sdkMakeLevel(t){
-  const reg=Array.from({length:6},()=>Array(6)),sol=Array(6);
-  for(let r=0;r<6;r++)for(let c=0;c<6;c++){
-    const [nr,nc]=sdkTransformCell(r,c,t);reg[nr][nc]=SDK_BASE_REGIONS[r][c];
-  }
-  for(let r=0;r<6;r++){const [nr,nc]=sdkTransformCell(r,SDK_BASE_SOLUTION[r],t);sol[nr]=nc;}
-  return {regions:reg.map(row=>row.join('')),solution:sol,animal:SDK_ANIMALS[t%SDK_ANIMALS.length],palette:SDK_PALETTES[t%SDK_PALETTES.length]};
+/* Independent in-app sanity check: every published board must have exactly one solution.
+   A solution has one cell in each row/column/region and no animals touching diagonally/orthogonally. */
+function sdkSolutionCount(lv,limit=2){
+  let count=0;
+  const usedCols=new Set(),usedRegs=new Set();
+  const walk=(r,prevC)=>{
+    if(count>=limit)return;
+    if(r===6){count++;return;}
+    for(let c=0;c<6;c++){
+      const reg=lv.regions[r][c];
+      if(usedCols.has(c)||usedRegs.has(reg))continue;
+      if(r>0&&Math.abs(c-prevC)<=1)continue;
+      usedCols.add(c);usedRegs.add(reg);walk(r+1,c);usedCols.delete(c);usedRegs.delete(reg);
+    }
+  };
+  walk(0,-9);return count;
 }
-const SDK_LEVELS=Array.from({length:8},(_,i)=>sdkMakeLevel(i));
+SDK_LEVELS.forEach((lv,i)=>{if(sdkSolutionCount(lv)!==1)console.error('Sudoku-brett har ikke unik løsning',i+1);});
 GAMES.sdk.n=SDK_LEVELS.length;
 
 let sdkCur=0,sdkMode='x',sdkX=new Set(),sdkFound=new Set(),sdkHintTimer=null;
@@ -1717,27 +1720,18 @@
   $('#sdk-mode-x').classList.toggle('sel',sdkMode==='x');
   $('#sdk-mode-animal').classList.toggle('sel',sdkMode==='animal');
 }
-function sdkAutoCross(r,c){
-  const lv=SDK_LEVELS[sdkCur],region=lv.regions[r][c];
-  for(let rr=0;rr<6;rr++)for(let cc=0;cc<6;cc++){
-    const k=sdkKey(rr,cc);
-    if(sdkFound.has(k)||sdkIsSolution(rr,cc))continue;
-    const touching=Math.max(Math.abs(rr-r),Math.abs(cc-c))<=1;
-    if(rr===r||cc===c||lv.regions[rr][cc]===region||touching)sdkX.add(k);
-  }
-}
 function sdkCellClick(r,c,el){
   const k=sdkKey(r,c);el.classList.remove('sdk-hint');
   if(sdkFound.has(k))return;
   if(sdkMode==='x'){
-    if(sdkIsSolution(r,c)){sfx('click');sdkScheduleHint();return;}
+    /* X marks are the child's own notes. They may be right or wrong and are always toggleable. */
     if(sdkX.has(k))sdkX.delete(k);else sdkX.add(k);
     sfx('click');sdkRender();return;
   }
   if(!sdkIsSolution(r,c)){
     el.classList.remove('sdk-wrong');void el.offsetWidth;el.classList.add('sdk-wrong');sfx('hmm');sdkScheduleHint();return;
   }
-  sdkX.delete(k);sdkFound.add(k);sdkAutoCross(r,c);sfx('snap');sdkRender();
+  sdkX.delete(k);sdkFound.add(k);sfx('snap');sdkRender();
   if(sdkFound.size===6)setTimeout(()=>award('sdk',sdkCur,()=>{if(sdkCur+1<SDK_LEVELS.length)openSdk(sdkCur+1);else goHome();}),420);
 }
 function sdkRender(){
@@ -1880,9 +1874,11 @@
   citEnsureExtras(); save(); citRenderTools(); citRenderHelp(); citFitBoard(); citRenderTiles(); citRenderPeople(); citHud(); citUpdateUndoButton();
 }
 function citFitBoard(){
-  const g=$('#cit-grid'), tools=$('#cit-tools'), right=$('#cit-right'), scr=$('#g-cit'), help=$('#cit-help');
+  const g=$('#cit-grid'), tools=$('#cit-tools'), right=$('#cit-right'), spacer=$('#cit-safe-spacer'), scr=$('#g-cit'), help=$('#cit-help');
   const vw=scr.clientWidth||innerWidth, vh=scr.clientHeight||innerHeight;
-  const sideW=tools.offsetWidth + right.offsetWidth;
+  const portrait=matchMedia('(orientation:portrait)').matches;
+  if(spacer)spacer.style.width=portrait?right.offsetWidth+'px':'0px';
+  const sideW=tools.offsetWidth + right.offsetWidth + (spacer?spacer.offsetWidth:0);
   const topReserve=Math.max(70, help?(help.offsetTop+help.offsetHeight+8):70);
   const byW=Math.floor((vw - 68 - sideW - 24) / CITY_CFG.COLS);
   const byH=Math.floor((vh - topReserve - 6) / CITY_CFG.ROWS);
@@ -2242,7 +2238,14 @@
 (()=>{
   const g=$('#cit-grid');
   const cellOf=e=>{
-    const r=g.getBoundingClientRect(), c=Math.floor((e.clientX-r.left)/citTs), rr=Math.floor((e.clientY-r.top)/citTs);
+    const r=g.getBoundingClientRect();
+    /* In portrait the city screen is CSS-rotated 90deg. getBoundingClientRect() is therefore
+       in physical screen coordinates, while city row/column coordinates remain unrotated.
+       Invert that transform before converting the pointer to a tile. */
+    const rotated=matchMedia('(orientation:portrait)').matches && getComputedStyle($('#g-cit')).transform!=='none';
+    const localX=rotated ? (e.clientY-r.top) : (e.clientX-r.left);
+    const localY=rotated ? (r.right-e.clientX) : (e.clientY-r.top);
+    const c=Math.floor(localX/citTs), rr=Math.floor(localY/citTs);
     if(rr<0||c<0||rr>=CITY_CFG.ROWS||c>=CITY_CFG.COLS)return null; return citIdx(rr,c);
   };
   const paintable=()=>['R','A','W','E','X'].includes(citTool);
